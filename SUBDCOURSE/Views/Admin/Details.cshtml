@model Moto

<h2>Смартфон @Model.Name</h2>
<div>
    <dl class="dl-horizontal">
        <dt>Цена</dt>
        <dd>@Model.Price</dd>

        <dt>Описание</dt>
        <dd>@Model.LongDesc</dd>
    </dl>
</div>
